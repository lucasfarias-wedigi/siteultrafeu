import type { Props } from "../components/search/Filters.tsx";
import Component from "../components/search/Filters.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
